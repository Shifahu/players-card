const { JSDOM } = require('jsdom');

const {
    getPlayerStats,
    getPlayerImg,
    calculatePlayerStats,
    playersPosition,
    changePlayer,
    eventListener,
    loadPlayers,
  } = require('./app.js');

  
  
  
  

  
  